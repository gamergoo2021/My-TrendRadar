# TrendRadar - GitHub 使用与维护手册

这份文档旨在帮助您管理 TrendRadar 的代码版本，实现**自动云端运行**（保活）和**同步作者的最新功能**。

## 核心概念图解

您的代码环境现在由三部分组成：

1.  **💻 本地 (Mac)**: 您的控制台。您在这里修改配置、测试运行。
2.  **☁️ Origin (您的 GitHub)**: 您的云端备份。负责运行 GitHub Actions 进行每日自动推送。
3.  **🌊 Upstream (原作者 GitHub)**: 水源地。提供最新的功能更新。

---

## 场景一：日常维护 (自动续命)

**目的**: 将本地的修改备份到云端，并告诉 GitHub "我还活着"，防止自动任务被暂停。
**频率**: 建议每月至少操作一次（随便改个标点符号提交都行）。

在终端中执行以下三步：

```bash
# 1. 添加所有修改到暂存区
git add .

# 2. 提交修改并写下备注 (备注内容随意)
git commit -m "日常维护: 更新配置"

# 3. 推送到您的 GitHub 仓库
git push
```

> **成功标志**: 看到 `To https://github.com/gamergoo2021/My-TrendRadar.git` 和 `[new branch]` 或 `master -> master` 等字样。

---

## 场景二：获取新功能 (同步作者)

**目的**: 当原作者发布了新版本（v5.1, v6.0 等）时，将新功能合并到您的项目中。
**频率**: 按需执行。

```bash
# 1. 从原作者仓库 (upstream) 拉取最新代码
git pull upstream master

# 2. 将更新后的代码推送到您的 GitHub (origin)
git push
```

> **注意**: 如果 `git pull` 提示冲突 (CONNECT)，通常是因为您修改了代码文件。如果只改配置文件一般不会冲突。

---

## 场景三：检查状态 (哪里出问题了？)

如果您不确定当前是什么情况，随时运行这个命令：

```bash
git status
```

*   **"nothing to commit, working tree clean"**: 一切干净，无需操作。
*   **"Changes not staged for commit"**: 您有修改没提交（去执行场景一）。
*   **"Your branch is ahead of 'origin/master'"**: 您本地有新东西没推送到云端（去执行 `git push`）。

---

## 备忘录：常用命令速查

| 您的意图 | 命令 |
| :--- | :--- |
| **我要备份/触发自动运行** | `git add .` -> `git commit -m "更新"` -> `git push` |
| **我要更新作者的新功能** | `git pull upstream master` -> `git push` |
| **查看我都改了什么** | `git status` |
| **查看连接了哪些仓库** | `git remote -v` |
